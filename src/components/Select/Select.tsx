import './select.css'

export const Select = () => {
  return (
    <select id="select">
      <option className='option'>Si</option>
      <option>No</option>
      <option>No Disponible</option>
    </select>
  )
}
